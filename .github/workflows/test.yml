name: test

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  latest:
    runs-on: ubuntu-latest
    container: thinca/vim:latest-ubuntu
    steps:
      - uses: actions/checkout@v2
      - name: setup
        run: |
          #apt install make git
          vim --version
          make setup
      - name: lint
        run: make lint
      - name: test
        run: make test
